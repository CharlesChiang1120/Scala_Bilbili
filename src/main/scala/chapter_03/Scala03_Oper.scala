package chapter_03

object Scala03_Oper {
    def main(args: Array[String]): Unit = {

      // TODO 運算符
      // 位運算符
      // 二進制運算符
      // byte => 8位 => 0000 0001
      //             => 0000 0101
      // HashMap 的數據如何存放底層結構中
      // 存儲容量必須為2的N次方，所以擴容時必須為2倍
      // key => key.hashcode => hash => index
      // index = hash(key.hashCode()) & (length - 1)
      // Kafka 中的數據如何存放在 topic 的 partition 中
      // key => %取餘 => partition


    }
  }
